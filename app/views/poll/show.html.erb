<p id="notice"><%= notice %></p>

<h1>Result</h1>

<table>
  <thead>
    <tr>
      <th>Team name</th>
      <th>Votes</th>
      <th>Percent</th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= team.votes %></td>
        <td><%= ((team.votes / @votes_count.to_f) * 100).round(1) %>%</td>
      </tr>
    <% end %>
  </tbody>
</table>

<strong>Total: </strong><%= @votes_count %>
